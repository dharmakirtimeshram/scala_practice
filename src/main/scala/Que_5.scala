object Que_5 {
  def main(args:Array[String]):Unit={

  }
//Print the reverse of a given list using a "while" loop.
    var arr = Array(10,20,45,50,62,75,80)
    var len = (arr.length) - 1

  while(len>=0)
    {
      println(arr(len))
      len = len - 1
    }

}
