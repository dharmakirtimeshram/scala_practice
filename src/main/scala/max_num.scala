object max_num {
  def main(args:Array[String]):Unit={

  }

//Maximum Element:
  // Determine the index of the maximum element in the array [14,5,8,12,9] using linear search.

  var arr = Array(14,5,8,12,9)
  var max = arr(0)
  for(i<- 0 until arr.length){
       if(max<arr(i))
       {
         max==arr(i)

       }

  }
  print(max)




























}
